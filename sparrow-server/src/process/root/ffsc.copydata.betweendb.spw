process "ffsc.copydata.betweendb"
{
	try
	{
		copydata as "copydata" from "${ffsc-db}" to "${ffsc-db}" with-batch "1000" using
		{
			"select * from fftc;
			insert ignore into BasicDataMapping_FFSC_Traffic values"
		}

	}
	catch
	{
	}
	finally
	{
	}
}