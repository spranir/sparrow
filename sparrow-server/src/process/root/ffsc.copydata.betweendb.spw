process "ffsc.copydata.betweendb"
{
	try
	{
		copydata as "copydata" from "${ffsc-db}" to "${ffsc-db}" using
		{
			"select * from BasicDataMapping_FFSC_Customers;
			insert ignore into ffc values"
		}

	}
	catch
	{
	}
	finally
	{
	}
}