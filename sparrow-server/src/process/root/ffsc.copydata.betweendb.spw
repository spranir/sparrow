process "ffsc.model.training"
{
	try
	{
		trainseq as "trainseq" from "${ffsc-db}" to "${ffsc-db}" using
		{
			"select * from t;
			insert ignore into v values"
		}

	}
	catch
	{
	}
	finally
	{
	}
}