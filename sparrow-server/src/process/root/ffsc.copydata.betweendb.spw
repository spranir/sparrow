process "ffsc.model.training"
{
	try
	{
		copydata as "trainseq" from "${ffsc-db}" to "${ffsc-db}" with-batch "100" using
		{
			"select * from BasicDataMapping_FFSC_Customers;
			insert ignore into ffc values"
		}

	}
	catch
	{
	}
	finally
	{
	}
}